apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: revenue-agent-ingress
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: revenue-agent-service
            port:
              number: 80
